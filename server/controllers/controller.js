// connect models

const Topic = require('../models/topics.model')

async function postTopics () {
  try {
    // const output = await
  } catch (err) {
    console.error(err.stack);
  }
}

async function getTopics () {
  try {
    // const output = await
  } catch (err) {
    console.error(err.stack);
  }
}

module.exports = { postTopics, getTopics }